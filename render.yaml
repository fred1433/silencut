services:
  - type: web
    name: silencut
    runtime: docker
    repo: https://github.com/YOUR_USERNAME/silencut
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
    disk:
      name: silencut-storage
      mountPath: /app/webapp/outputs
      sizeGB: 1